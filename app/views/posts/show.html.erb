<h1><%= @post.title %></h1>
<small><%= @post.created_at.strftime("%b %d. %y") %></small>
<h3><%= @post.body %></h3>
<br>
<%= link_to "Edit post",edit_post_path %>
<br>
<%= link_to "Delete this", @post, method: :delete, data: { confirm: "are you sure?" } %>
